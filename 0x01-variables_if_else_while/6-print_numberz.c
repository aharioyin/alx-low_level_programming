#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 * using putchar
 *
 * Return: Always 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		purchar(n);
	purchar('\n');
	return (0);
}
