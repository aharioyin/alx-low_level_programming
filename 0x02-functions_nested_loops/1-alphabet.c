#include "main.h"
/**
 * main -  prints the alphabet, in lowercase
 *
 * Return: 0
 */
int main(void)
{
	char low;

	if (low = 'a'; low <= 'z'; low++)
		_putchar(low);

	_putchar('\n');

	return (0);
}
