#include "main.h"
/**
 * print_alphabet_10x - print 10 times the alphabeth in lowercase
 */
void print_alphabet_x10(void)
{
	int i = 0, low;

	while (i < 1)

	for (low = 'a'; low <= 'z'; low++)
	{
		_putchar(low);
		low++;
	}
